// Generated by CoffeeScript 1.7.1
(function() {
  var app, articleApp, getArtile;

  window.app = app = {};

  app.urls = {
    getArticle: "/webservice/article"
  };

  getArtile = function() {
    return $.ajax({
      url: app.urls.getArticle + "?hash=" + window.userHash + "&articleid=" + window.article_id,
      type: "GET",
      dataType: "json",
      async: false,
      success: function(data) {
        var article;
        console.log(data);
        window.article = data;
        article = data;
        return article;
      }
    });
  };

  articleApp = angular.module('articleApp', []);

  articleApp.controller('mainCtrl', function($scope) {
    var articleData;
    articleData = getArtile();
    window.article1 = articleData.responseJSON.article;
    $scope.article = articleData.responseJSON.article;
    $('.article-container .article .content').html(articleData.responseJSON.article.content);
  });

}).call(this);
