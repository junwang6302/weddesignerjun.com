// Generated by CoffeeScript 1.7.1
(function() {
  var app, articleListApp, getArtiles;

  window.app = app = {};

  app.urls = {
    getArticles: "/webservice/getarticles"
  };

  getArtiles = function() {
    return $.ajax({
      url: app.urls.getArticles + "?hash=" + window.userHash,
      type: "GET",
      dataType: "json",
      async: false,
      success: function(data) {
        var articles;
        console.log(data);
        window.articles = data;
        articles = data;
        return articles;
      }
    });
  };

  articleListApp = angular.module('articleListApp', []);

  articleListApp.controller('mainCtrl', function($scope) {
    var articles;
    articles = getArtiles();
    window.articles1 = articles.responseJSON.articles;
    $scope.articles = articles.responseJSON.articles;
  });

}).call(this);
